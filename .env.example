# Postgres connection string (Coolify-managed Postgres in production).
DATABASE_URL=postgres://user:password@localhost:5432/cahayoyo_cockpit

# Session signing secret. Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=replace-with-a-long-random-string

# AES-256-GCM key for vault entries (used from Phase 10; never logged, never in the DB).
VAULT_ENCRYPTION_KEY=replace-with-a-32-byte-base64-key
